var secretKey = 'PUT THE KEY HERE';

module.exports = secretKey;

/**
 * Instructions:
 *
 *  Create a new file names stripe.config.js and replace the above variable with
 *  your stripe secret key. 
 */
